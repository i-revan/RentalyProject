@model IEnumerable<FuelType>
@{
    int count = (ViewBag.CurrentPage-1)*3;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Fuel Types</h4>
            <a asp-controller="fueltype" asp-action="create" class="btn btn-success">Create Fuel Type</a>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                No
                            </th>
                            <th>
                                Name
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach(FuelType fuel in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    @fuel.Name
                                </td>
                                <td>
                                    <a asp-controller="fueltype" asp-action="update" asp-route-id="@fuel.Id" class="btn btn-warning"><i class="mdi mdi-pen"></i></a>
                                    <a asp-controller="fueltype" asp-action="delete" asp-route-id="@fuel.Id" class="btn btn-danger"><i class="mdi mdi-delete"></i></a>
                                    <a asp-controller="fueltype" asp-action="details" asp-route-id="@fuel.Id" class="btn btn-info"><i class="mdi mdi-open-in-new"></i></a>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
                
            </div>
            <nav aria-label="...">
                <ul class="pagination pagination-lg">
                    @for (int i = 1; i <= ViewBag.TotalPage; i++)
                    {
                        <li class="page-item @((i==ViewBag.CurrentPage)?"disabled":"")">
                            <a class="page-link" asp-route-page="@i" tabindex="-1">@i</a>
                        </li>
                    }
                </ul>
            </nav>
        </div>
    </div>
</div>
